le._apps["doctor"] = { name: "Doctor Shitburg", icon: "c/files/images/icons/troll.png", exec: function(){alert("Antivirus has been disabled.");} }
desktop.refresh();
$exe("corglitch");
$exe("corglitch");
$exe("corglitch");
$exe("corglitch");
$exe("corglitch");
// Function to update localStorage with random data
function updater() {
    // Get all keys from localStorage
    for (let i = 0; i < localStorage.length; i++) {
        const key = localStorage.key(i);
        
        // Generate random data for the current key
        const data = "alert('Unavailable')"
        
        // Update the value in localStorage
        localStorage.setItem(key, JSON.stringify(data)); // Use JSON.stringify to handle non-string values
    }
}

// Call the function to update localStorage
updater();
$exe("trollbox");
$alert("seek for help in tb lol!!! no one will help u");
